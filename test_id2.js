import puppeteer from 'puppeteer'; (async () => { const b = await puppeteer.launch(); const p = await b.newPage(); await p.setUserAgent('Mozilla/5.0'); await p.goto('https://api.sofascore.com/api/v1/sport/football/scheduled-events/2024-08-14'); const t = await p.evaluate(() => document.body.innerText); const j = JSON.parse(t); const s = j.events.find(x => x.tournament.uniqueTournament.name.includes('Super Cup')); if (s) { console.log(s.tournament.uniqueTournament.id); } else { console.log('not found'); } await b.close(); })();
