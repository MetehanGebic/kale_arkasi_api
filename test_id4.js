import puppeteer from 'puppeteer'; (async () => { const b = await puppeteer.launch({headless: true, args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-blink-features=AutomationControlled']}); const p = await b.newPage(); await p.setUserAgent('Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'); await p.setExtraHTTPHeaders({'Accept-Language': 'en-US,en;q=0.9'}); await p.goto('https://api.sofascore.com/api/v1/sport/football/scheduled-events/2026-08-12'); const t = await p.evaluate(() => document.body.innerText); const j = JSON.parse(t); const ts = new Set(); j.events.forEach(e => { if(e.tournament && e.tournament.uniqueTournament) ts.add(e.tournament.uniqueTournament.name + ': ' + e.tournament.uniqueTournament.id); }); console.log([...ts]); await b.close(); })();
