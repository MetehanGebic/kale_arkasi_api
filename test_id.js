import puppeteer from 'puppeteer'; (async () => { const b = await puppeteer.launch(); const p = await b.newPage(); await p.setUserAgent('Mozilla/5.0'); await p.goto('https://api.sofascore.com/api/v1/config/unique-tournaments/en/football'); const t = await p.evaluate(() => document.body.innerText); console.log(t.substring(0,200)); const j = JSON.parse(t); const s = j.uniqueTournaments.find(x => x.name.includes('Super Cup') && x.category.name === 'Europe'); console.log(s); await b.close(); })();
